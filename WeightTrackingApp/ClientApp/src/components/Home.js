import React from 'react';
import { connect } from 'react-redux';

const Home = () => (
  <div>
    <h2>This is a weight tracking app</h2>
      <p>It can track your weight and the exercise program that you may have used thus far</p>
  </div>
);

export default Home;
